secret is the next random number

shell result is:
  - 0 when user wins
  - 1 when user looses

has more attempts when guess count < 3:
  otherwise user looses

user wins when guess is secret

guess is a number from the user with prompt:
  - "Guess a number:" when launched
  - "Wrong. Guess again: " when has more attempts

a user message is:
  - "You win!" when user wins
  - "You lost." when user looses 

