the mouse:
  (= a human input device where:
    type == "mouse"
  position (= a coordinate:
    top (=0 (++ mouse move top
    left (=0 (++ mouse move left
  target is a UI frame where:
    z-index 
    top 
